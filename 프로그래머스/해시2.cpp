#include <string>
#include <vector>

using namespace std;

bool solution(vector<string> phone_book) {
	bool answer = true;
	unordered_map<string, int> hash;
	//문자열 추출??
	for (int i = 0; i < phone_book.size(); i++) {

	}
	return answer;
}